/*
Basically hashing is like a function that takes into account of the 
data and convert them into small numbers,
some example hash funtions are:

index= num % n  for intergers

index= str[0]*x^0 + str[1]*x^1 + str[2]*x^2... for the string values

There is also a Universal Hashing Funtion



*/